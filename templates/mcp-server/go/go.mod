// Placeholder module path. Replace `github.com/example/project` with the
// project's real module path during bootstrap; update every import site.
module github.com/example/project/mcp-server

go 1.22
